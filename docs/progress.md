# 진행상황 요약 (작업 핸드오프)

> 이 문서는 **작업 종료 시마다 갱신**한다. 다음 세션이 이 문서만 읽고 바로 이어서 작업할 수 있도록 유지한다.
> 상세 아키텍처는 `CLAUDE.md`, 운영/콘텐츠 정책은 `AGENTS.md` 참고.

> 🆕 **중복 등록 '취소대상' 표시(2026-07-19, 방향 조건 보정)**: 같은 번호가 **더 앞 날짜 수업에서 결제 확정(paid/confirmed)** 됐는데 **그보다 뒤 날짜**의 살아있는 신청(대기·선착순·결제 안내 중)이 있으면 구분 배지 옆에 빨간 **"취소대상"** 배지(`isCancelCandidate` — 확정건이 취소/환불됐으면 대상 아님, **뒤 수업이 확정된 경우 앞 날짜 대기는 여전히 유효할 수 있어 표시 안 함**(1차 구현이 방향 무관이라 백관장 지적으로 보정), node 5케이스 검증). 상태 필터에 **"🚩 취소대상"** 칩 추가 — 전체 보기와 조합하면 정리할 신청을 한 번에 모아 '문자 없이 처리'로 정리 가능. 자동 취소는 안 함(관리자 판단·수동 처리). 프론트만, 계약 23 PASS.
>
> 🆕 **문자 없이 취소 처리(2026-07-19)**: 취소 처리 모달에 **"문자 없이 처리"** 버튼(취소 액션에서만 노출, `allowSilent`) — 누르면 취소 안내 문자 없이 상태만 변경(`SEND_SILENT` 심볼 → `silent:true` 전달). 서버 `updateReservation(silent)`: silent면 모든 안내 문자 발송/예약(리마인드 포함)을 건너뛰되 **걸려있던 예약 문자 취소 등 정리 작업은 그대로 수행**(결제완료 분기의 결제 리마인드 취소도 유지). admin-reservations 배포 완료, 계약 23 PASS. (참고: silent 옵션은 다른 액션에도 서버는 이미 지원 — 모달 노출만 cancel 한정.)
>
> 🆕 **전체 보기에서도 일괄 처리 허용(2026-07-19)**: 전체 수강생 보기에서 숨기던 일괄 처리 바를 다시 노출. 안전장치 — 문자가 나가는 액션(결제 안내/여석/마감/환불/취소 등)은 **선택자들의 class_id가 2개 이상 섞이면 차단**("같은 수업의 신청자끼리만 선택하세요"), 미리보기(previewMessage)도 selectedClassId가 아니라 **선택자들의 수업 기준**으로 호출(`confirmSmsBeforeSend`에 classId 인자 추가). 문자 없는 처리(대기/예약 거부)와 직접 작성 문자는 수업 혼재 선택도 허용. 프론트만, 계약 23 PASS.
>
> 🆕 **구분 배지 색·라벨 통일(2026-07-19)**: 수업별 보기(groupBadgeHtml)와 전체 보기(globalBadgeHtml)의 배지 색이 달라 혼동(특히 수업별의 '선착순 N'이 결제 안내 중과 같은 주황) → **groupBadgeHtml·liveRank/seatNo 계산 삭제하고 globalBadgeHtml(seatRankLabel 기반) 하나로 통일**. 색 체계: 확정=초록, 결제 안내 중=주황(+경과 배지), 선착순 신청/확보=청록, 대기=파랑, 취소/불참=회색. 라벨도 "선착순 1"→"선착순 1위", "대기 1"→"대기 1순위"로 통일. 프론트만, 계약 23 PASS.
>
> 🆕 **전체 수강생 보기 + 상태 필터(2026-07-19)**: "📋 전체 수강생 보기" 토글 — 모든 수업의 신청자를 한 표에(수업일 컬럼 `data-th-class-date` 표시, 수업일→신청 시간순 정렬, 요약 카드는 전 수업 합계). 상태 필터 칩(전체/선착순/결제 안내 중/확정/대기/취소, `reservationCategory` — 선착순 확보는 '선착순'에 포함)은 수업별/전체 보기 공통 적용. 전체 보기에서는 **일괄 처리 바 숨김**(수업이 섞인 상태에서 문자 수정본(messageText)이 다른 날짜 수신자에게 잘못 가는 사고 방지 — 처리 작업은 수업별 보기에서). 검색 결과 클릭 시 자동으로 수업별 보기+필터 해제로 전환. 프론트만, 계약 23 PASS.
>
> 🆕 **환불 규정 + 환불 처리 버튼(2026-07-19)**: 선결제 전환에 맞춰 ① `index.html` FAQ에 환불 규정 신설 — "수업 시작 전 취소는 전액 환불"(단일 규정, 스마트스토어 구매확정 후 취소가 전액 환불만 지원되는 제약에 맞춤. 부분 환불 규정 없음) ② 관리자 벌크 바 **"환불 처리"** 버튼 — `cancelled`+`refunded`로 기록하고 새 문자 `payment_refunded`("환불 처리되었습니다, 결제 수단으로 환불 진행") 발송(확인 모달 경유, 모달 노트에 "실제 환불은 스마트스토어에서 별도 승인" 경고). `updateReservation`에 refunded 분기(expired 다음, cancelled보다 먼저 — 일반 취소 문자 대신 환불 안내만) + 예약 문자 자동 취소. 운영 절차: 스마트스토어에서 환불 승인 → 수강생 검색으로 찾기 → 환불 처리 버튼 → (대기자 있으면) 여석 안내. 함수 2개(solapi·admin) 배포+preview 실검증, 계약 23 PASS.
>
> 🆕 **수강생 검색(2026-07-19)**: 수업일별 현황 위에 검색창 — **모든 수업 대상**으로 이름/전화번호(부분 일치) 검색. 결과에 이름·전화·수업일·상태(seatRankLabel)가 뜨고, **클릭하면 해당 수업으로 전환 + 그 신청자 체크박스 자동 선택 + 노랑 하이라이트/스크롤** → 바로 '결제 완료 처리' 가능. 용도: 즉시 결제 전환 후 스마트스토어 결제자(이름+전화)를 어느 수업 신청자인지 빠르게 대조. 프론트만(`searchReservations`/`renderSearchResults`, adminData 재사용) — push로 Pages 자동. 계약 23 PASS.
>
> 🆕 **즉시 결제 전환 + 결제 기한 리마인드 자동 문자(2026-07-19)**: 무료 예약 후 잠수(미결제) 문제 대응으로 흐름 자체를 전환. ① **즉시 결제** — `submit-reservation`: 정원 내 신청은 이제 `applied`가 아니라 **`payment_target`+`sent`로 저장**(자리 즉시 점유 + 결제 시계 시작), 접수 문자(`reservation_success`)에 **결제 링크+기한을 바로 포함**("결제 완료 순 확정"). 대기 신청은 기존대로 무료(`waitlisted`). 대기 접수 문자에서 "1주일 전 발송" 문구 제거. `index.html` 타임라인 3단계·FAQ도 즉시 결제 문구로 갱신(계약 `assertNotIn 수업 일주일 전에 발송됩니다`). D-7 일괄 승인 흐름은 **기존 신청자(applied) 처리용으로 그대로 유지** — 8월 기존 신청자 결제 요청은 백관장이 기존 버튼으로 직접 발송 예정. ② **결제 기한 리마인드** — 새 타입 `payment_deadline_reminder`("결제 기한이 약 {remaining_hours}시간 남았습니다"+링크)를 **기한 절반 시점에 Solapi 예약 발송**: 발송 지점 = 즉시 결제 접수(submit), 결제 안내/여석 안내(updateReservation·bulkApprove·resendMessage, `schedulePaymentReminder` — 중복 예약 방지). **자동 취소**: 결제 완료 시(payment_completed 분기), 미결제 마감/취소/차단 시(`cancelScheduledFollowups` 기본 타입에 추가), **본인 취소 시**(`cancel-reservation`에 `cancelScheduledMessages` 신설 — 기존에 예약 문자 취소 없던 갭도 해소). ③ admin.html — 현황판에 "결제 기한 리마인드 문자" 행 추가, 경과 배지 기준 시각에 `reservation_success`(즉시 결제 접수 문자) 포함. 함수 4개(solapi·submit·admin·cancel) 배포, 새 템플릿 2종 preview 실검증, kstAfterHours node 왕복 검증, 계약 23 PASS. **미결제 자동 마감은 보류(백관장이 수동 마감 유지, 네이버API 자동화는 별도 검토). AGENTS.md의 결제 흐름 서술(D-7 프레임)은 백관장 확인 후 갱신 필요.**
>
> 🆕 **예약 거부(차단) 시스템 + 결제 기한 설정(2026-07-19)**: ① **예약 거부** — 벌크 바 "🚫 예약 거부(차단)" 버튼: 선택 신청자의 번호를 신규 테이블 **`public.blocked_phones`**(RLS on·anon 차단)에 등록하고 활성 예약은 **조용히 취소**(문자 없음, 예약된 후속 문자도 취소). `submit-reservation`이 차단 번호의 신규 신청을 403 거부("현재 이 번호로는 온라인 예약 신청을 받을 수 없습니다"). 번호 옆 "🚫 차단됨" 배지 클릭 → 해제(`unblockPhone`). ② **결제 기한 설정** — 신규 테이블 **`public.app_settings`**(key-value)의 `payment_deadline_hours`(기본 24, 1~72 검증). solapi가 발송/미리보기 때 `{payment_deadline_hours}` 치환자를 설정값으로 채움(`fetchAppSetting`, 실패 시 24 폴백). 기본 템플릿 'payment 안내'/'seat_opened'의 "24시간" 리터럴을 치환자로 교체 + **운영 DB에 저장돼 있던 백관장 수정본('payment 안내')의 "24시간" 리터럴도 치환자로 마이그레이션**(수정본이 템플릿보다 우선하므로 필수 — 실측 검증: 설정 6 → "안내 후 6시간까지" 발송 확인). 관리자 UI: 문자 발송 현황 헤더에 "결제 기한 [N]시간 저장" 입력(`saveSetting`). **경과 배지 이동**: '결제 안내 중' 배지(구분 칸) 옆에 기한 초과 시 "Nh 경과"(rose) 표시 — 기준이 24 고정에서 설정값으로 변경, 기존 결제 상태 칸 배지는 제거. 운영 DB에 테이블 2개 생성 완료(관리 API), 함수 3개(solapi·admin·submit) 배포 완료, 계약 22 PASS. **주의: 템플릿 수정본에 "24시간"을 다시 문자 그대로 쓰면 설정과 무관해짐 — {payment_deadline_hours} 유지 필요(모달 힌트에 안내됨).**
>
> 🆕 **벌크 액션 확인창을 문자 모달로 일원화(2026-07-19)**: 미결제 마감·취소·자리확보가 "일반 confirm → 문자 모달" 2단계라 운영자가 1단계 confirm만 보고 "문자 미리보기가 안 뜬다"고 혼동(스크린샷 제보). **기존 window.confirm 3개 제거**, 문자 모달이 유일한 확인 단계가 되도록 통합 — 상태 변경 경고는 모달 안 `#sms-modal-note`(액션별 `BULK_ACTION_NOTES`, 마감/취소는 ⚠️ 표시)로 이동. 서버/배포는 이상 없음 확인(Pages 최신 일치, solapi preview 실호출 OK — 참고: 이 프로젝트 Edge Function 환경의 `SUPABASE_SERVICE_ROLE_KEY`는 신형 `sb_secret_…` 형식이라 레거시 JWT로 내부 인증 테스트하면 실패함). '대기 처리'는 문자가 안 나가는 액션이라 모달 없음(정상). 계약 21 PASS. admin.html만 — push로 Pages 자동.
>
> 🆕 **문자 발송 현황에 발송 시각 표기(2026-07-19)**: 현황판 각 행의 발송 완료 표시에 **실제 발송 날짜·시간(KST)** 추가 — "✅ 전체 발송 완료 (이름들) · 7/18 14:32 발송" 형식(여러 시각이면 가장 최근, `sentLabel` — schedLabel과 동일한 +9h 변환 규칙). 구현: `admin-reservations` list의 message_logs select에 `sent_at` 추가 + `admin.html` `summarizeMessageStatus`가 `sentAt`(최근 sent 시각) 집계·표기. **부수 수정: 그동안 `sent_at`이 안 내려와 결제 안내 후 '24h 경과' 배지(`paymentSentAt`)가 동작하지 않던 버그가 이 select 추가로 고쳐짐.** node 5케이스 자체검증 + 계약 21 PASS. ✅ `admin-reservations` 재배포 완료(admin.html은 push로 Pages 자동).
>
> 🆕 **문자 발송 전 확인/수정 기능(2026-07-18)**: 관리자에서 나가는 모든 수동 문자 경로(벌크 액션 6종·선착순 일괄 승인·현황판 재발송·복습 영상)가 발송 전에 **실제 본문을 모달로 보여주고 "이 내용을 문자로 보내시겠습니까?" 확인**을 받는다. 본문은 textarea에서 **자유 수정 가능** — 확인한 본문이 `messageText`(override)로 서버에 전달돼 화면에 보인 그대로 발송된다(모달에서 취소하면 상태 변경 포함 전체 중단). 구현: ① `solapi-reservations` — `preview:true`면 발송 없이 채워진 본문만 반환, `overrideText` 있으면 템플릿 대신 그대로 발송 ② `admin-reservations` — 새 `previewMessage` 액션(classInfo+PAYMENT_LINK로 values 구성 → solapi preview 내부 호출), `updateReservation`/`bulkApprove`/`resendMessage`가 `messageText`를 받아 **즉시 발송분** notify에 전달(결제완료 시 후속 리마인드/복습 **예약 발송분은 템플릿 그대로** — 수정본은 즉시 발송분에만 적용) ③ `admin.html` — `#sms-modal` + `confirmSmsBeforeSend()` + `BULK_ACTION_MESSAGE_TYPE` 매핑(waitlist는 문자 없음), seat-opened의 기존 confirm은 모달이 대체(cancel/payment-expired/seat-secured의 상태 경고 confirm은 유지 — 모달과 2단 확인). 자동 발송(고객 신청 시 접수 문자, D-7 운영자 리마인더, 예약 시각 도래 발송)은 확인 대상 아님. 계약 테스트 21개 PASS(신규 `test_sms_preview_confirm_and_edit_before_send`). **✅ 배포 완료(2026-07-18): `solapi-reservations`+`admin-reservations` 두 함수 배포, 인증 게이트 스모크(틀린 비번→401) 통과**(admin.html은 push로 Pages 자동). 같은 날 후속으로 **'✉️ 직접 작성 문자 보내기' 버튼 신설**: 액션 바 맨 끝(보라색), 체크한 신청자에게 **상태 변경 없이** 관리자가 모달에서 직접 쓴 본문만 발송. 구현: solapi `custom` 타입(빈 템플릿, overrideText 필수 — 빈 본문은 `text is required` 400), admin-reservations `RESENDABLE_TYPES`에 `custom` 추가+본문 필수 검증(`resendMessage` 재사용, 새 액션 없음), admin.html `handleCustomSms()`(빈 textarea로 모달 오픈, 빈 본문 발송 시 alert). 현황판에 custom 행은 없음(message_logs엔 기록됨). 두 함수 재배포+401 스모크 통과, 계약 21 PASS. 같은 날 **문자 템플릿 영구 수정 기능 신설**: 문자 발송 현황 헤더의 **"📝 문자 템플릿 수정"** 버튼 → 모달에서 문자 종류 선택·본문 수정·저장/기본값 복원. 저장본은 신규 테이블 **`public.message_templates`**(message_type PK, body — RLS on·정책 없음=anon 차단)에 저장되고, `solapi-reservations`가 발송/미리보기 때 **DB 수정본 우선, 없으면 코드 기본 템플릿** 사용(`fetchTemplateOverrides`, DB 실패 시 기본값 폴백으로 발송 지속). 수정/조회 API는 solapi에 `listTemplates`/`saveTemplate`(관리자 비밀번호 인증, admin.html `callSolapiApi`로 직접 호출, 빈 본문 저장=행 삭제=기본값 복원, custom 타입은 편집 제외). **✅ 운영 DB에 테이블 생성 완료(2026-07-18, 관리 API로 적용)** + solapi 재배포 완료. 접수 문자·D-7 리마인더 등 자동 발송 문자도 템플릿 수정의 적용을 받는다. 같은 날 **네이버 스마트스토어 결제 자동 연동 검토** 완료 → `docs/naver-commerce-api-review.md` (결론: 조건부 가능 — 커머스API로 이름+전화 조회 가능하나 웹훅 없음+IP 화이트리스트라 고정 IP 워커 필요, 현 규모엔 과설계 가능성. 구현은 백관장 결정 대기).
>
> 🆕 **문자 발송현황 예약시각 UTC 표시 버그 수정(2026-07-11)**: 현황판의 예약 발송 시각(`schedLabel`)이 "7/10 09:00"·"7/11 07:00"처럼 **UTC로 표시**되던 버그 수정. 원인: Edge Function은 `…T18:00:00+09:00`(KST)으로 저장하지만 `scheduled_at`이 `timestamptz`라 PostgREST가 **UTC(+00:00)로 돌려주는데**, `schedLabel`이 문자열 숫자를 정규식으로 그대로 잘라 표기했기 때문. 과거/미래 판정(`new Date(iso)`)은 타임존을 올바르게 해석해 **"예약시각 지남" 판정 자체는 정확했음**(예: 7/11 수업 13~16시 → 복습자료 문자는 종료시각 16:00 KST 예약 = UTC 07:00 표시). 수정: `schedLabel`이 UTC ms에 +9h 해서 KST로 표기(입력이 +09:00/+00:00 어느 쪽이어도 동일). admin.html 프론트만 — push로 Pages 자동, 함수·DB 무변경. node 자체검증 5케이스 + 계약 테스트 PASS.
>
> 🆕 **'선착순 확보(결제 링크 X)' 버튼 신설(2026-07-07)**: 선착순 취소로 여석이 생겼는데 **수업이 아직 한참 남은** 대기자에게 자리를 넘길 때, 결제 링크를 지금 주면 안 됨(결제는 D-7). 그래서 **결제 링크 없이 '자리 확보'만 통보**하는 버튼·문자 신설. 상태는 **새 status 없이 `payment_target` 재사용**(자리 점유·정원 계산·D-7 리마인더 대상 로직 그대로) + `payment_status='pending'` 유지(24h 결제 시계 안 돌림, `seat_opened`/`payment 안내`는 `sent`로 저장돼 구분됨). 변경 4파일: ① `solapi-reservations` 새 타입 **`seat_secured`** 템플릿(결제 링크 없음, "자리 확보했고 결제 링크는 1주일 전에" 안내) ② `admin-reservations.updateReservation` payment_target 분기에서 `notify==='seat_secured'`면 링크 빼고 발송 + `RESENDABLE_TYPES` 추가 ③ `admin.html` 벌크 버튼 **"여석 안내 (결제 링크 X)"**(2026-07-07 라벨 통일: 기존 "여석 안내"→**"여석 안내 (결제 링크 O)"**[seat-opened], 신규→**"여석 안내 (결제 링크 X)"**[seat-secured] — 둘은 결제 링크 유무만 다른 같은 기능이라 이름 통일)(`data-bulk-action="seat-secured"` → `payment_target`+`pending`, `notify:'seat_secured'`, 확인창) + 배지 분기: `payment_target`+`sent`→"결제 안내 중"(기존), `payment_target`+`pending`→**"선착순 확보 N"**(신청순 좌석번호, `groupBadgeHtml`/`seatRankLabel` 양쪽) + 현황판 "자리 확보 문자" 행 ④ 계약 테스트 무변경(20 PASS). 좌석번호/배지 로직 node 자체검증 통과. **이후 흐름:** D-7 때 백관장이 기존 "결제 안내 대상으로 지정" 누르면 `sent`+진짜 결제 링크(`payment 안내`) 발송으로 자연 연결. **✅ 배포 완료(2026-07-07): `solapi-reservations`+`admin-reservations` 두 함수 `npx supabase functions deploy … --no-verify-jwt`로 배포**(admin.html은 push로 Pages 자동). 인증 게이트 스모크(틀린 비번→401) 양쪽 통과. **남은 것: 백관장이 관리자에서 대기자 체크→"선착순 확보 (결제 링크 X)" 눌러 실문자(seat_secured) 수신 + 배지 "선착순 확보 N" 표시 실확인.** (배포 도구 메모: 이 PC엔 supabase CLI 바이너리 없음 → **`npx supabase` 사용**(node24/npm11 설치됨), 로그인 세션 살아있어 재로그인 불필요.)
>
> 🆕 **달력에 오늘 날짜 표기(2026-07-06)**: 고객(`index.html`)·관리자(`admin.html`) 월간 달력에서 **오늘 날짜 칸을 파란 pill(숫자에 `bg-blue-600` 원) + 파란 링(`ring-2 ring-blue-500`)**으로 표시. index는 원래 오늘을 `bg-red-500`/`ring-red-400`(빨강)으로 그려 공휴일·일요일 빨강과 혼동됐어서 **파랑으로 변경**(공휴일과 구분). admin은 오늘 표기가 없어 **신규 추가**(`renderAdminCalendar`에 `isToday = year/month/d === now` 판정). 프론트만 — push로 Pages 자동, 함수·DB 무변경. 계약 20개 PASS(admin `isToday` 회귀 계약 추가).
>
> 🆕 **대기 접수 문자에 대기 순위 표기(2026-07-04)**: 대기로 접수된 신청자 문자(`reservation_waitlist`)에 **신청 시점 대기 순위** 안내 추가. `submit-reservation`이 `waitlistRank = activeCount(본인 제외 활성) - capacity + 1`을 계산해 `sendReceivedSms` → solapi `values.waitlist_rank`로 전달, 템플릿 "현재 정원이 모두 차서 **대기 {waitlist_rank}순위**로 접수되었습니다 (현재 기준이며, 앞 신청자가 취소하면 순번은 앞당겨집니다)". **AGENTS.md '고객 순번 비노출' 정책의 예외**로 명시(상시 노출 화면/조회는 여전히 비공개, 문자 1회 안내만 허용 — 백관장 결정). 계약 20개 PASS + 순위 계산 node 검증. **✅ 배포 완료(2026-07-07): submit-reservation + solapi-reservations.**
>
> 🆕 **중복 신청 정책: '대기는 허용·선착순만 번호당 1건' + 여석 문자 회신 안내(2026-07-04)**: 같은 번호가 다른 날짜에 또 신청하던 문제(박흥수 01090013630 두 수업). **1차로 '번호당 활성 1건 전면 차단'했다가, 백관장 요청으로 재조정** → **대기 신청은 날짜별 무제한 허용, '선착순 자리' 신청만 번호당 1건**. ① `submit-reservation`: 신청 시점 그 수업의 활성수(본인 제외)로 선착순/대기 판정(`willWaitlist = capacity>0 && activeCount>=capacity`). **대기면** 중복검사 skip + `reservation_status='waitlisted'`로 저장. **선착순이면** 이 번호의 blocker(`reservation_status=in.(applied,payment_target,confirmed)`) 있으면 409 — 같은 수업이면 `SAME_CLASS_MESSAGE`, 다른 수업이면 `SEAT_DUP_MESSAGE`("…다른 수업의 선착순 자리는 신청할 수 없습니다. 자리가 찬 수업은 대기로…"). 신청 시점 판정을 status에 반영해도 `bulkApprove`가 created_at순 재배정(applied/waitlisted 동일 후보)이라 승인 흐름 무충돌. ② `schema.sql`: 유니크 인덱스 `(phone)`→**`(class_id, phone)` 원복**(같은 수업 중복만 DB 차단, 대기 여러 날 허용). **운영 DB는 원래 (class_id,phone)이라 손댈 것 없음 — 1차 안내했던 인덱스 교체 SQL은 실행 불필요/취소**. ③ `admin.html`: 선착순·확정(applied/payment_target/confirmed) 2건 이상 번호에 행 `중복 N건` 배지(대기 `waitlisted`는 정상이라 제외). **상단 빨강 배너는 제거**(요청) → 배지를 **버튼(`data-dup-phone`)으로** 만들어 클릭 시 그 번호의 신청 날짜별 순위(`seatRankLabel` — groupBadge와 동일 기준: 확정/결제 안내 중/선착순 N위/대기 N순위)를 `alert`로 표시. ④ `solapi-reservations` **seat_opened**(여석 안내)+**payment 안내**(결제 안내)에 "수강을 원하지 않으시면 이 문자로 회신 주세요" 안내 추가(여석=대기 명단 정리 / 결제=다음 대기자에게 자리 안내). **발신번호 확인: `SOLAPI_SENDER` 해시 == `ADMIN_PHONE` 해시 → 여석 문자 발신·회신 번호 = 01054167694(백관장 본인)**. 계약 테스트 20개 PASS + 판정 로직 node 6시나리오 검증. **✅ 배포 완료(2026-07-07): submit-reservation + solapi-reservations 재배포**(admin.html은 push로 Pages 자동).
>
> 🆕 **색상 테마 → 브랜드 블루 전환(2026-06-30)**: 공개 페이지 색상을 백관장 홈페이지 톤(브랜드 블루)으로 교체. **레이아웃·구조·폰트·텍스트·동작 무변경 — 색상 값만.** 핵심은 Tailwind config 팔레트 토큰 일괄 교체(`index`·`checkout`·`lookup` 동일 config): `brand` 주황→블루(`500 #EF6B2B→#0A4DB0`, `600/700 #D95715·#B2430B→#083C8C`), `brand-50/100`(연주황 틴트)→라이트 서피스/보더 `#F6F8FB`/`#E4E9F0`(지시서 규칙4), `cream-50`(웜 아이보리 캔버스)→흰색 `#FFFFFF`, `ink-900/800 #0F1113·#1A1D21→#15171C·#1D1F25`(다크 섹션·서피스), `ink-700~100`은 이미 쿨 뉴트럴이라 유지. **별도 처리**: ① 다크 섹션(APPLY CTA) 포인트색=브라이트 블루 `#6AA4FF`(글로우·eyebrow), 솔리드 버튼은 `#0A4DB0` 유지(흰글씨 대비) ② 달력 규칙6 — 예약가능 셀 솔리드 `#0A4DB0`+흰글씨+보조 `#CFE0FF`, 만석/대기 셀 다크칩 `#3A3F4A`+`#E4E9F0`/`#A6ABB3`(hover `#2A2E36`), 범례 대기점 `#3A3F4A` ③ 대기 안내 amber 박스→중립 `#F6F8FB`/`#E4E9F0`/`text-ink-900`. `email/index.html`(인라인 hex)도 동일 hex→hex 매핑으로 일괄 치환(다크 배경 위 강조 없음). 작업지시서·시안: `~/Downloads/작업지시서_케틀벨페이지_색상변경/`. **계약 테스트 20개 PASS, 잔여 주황/웜 hex 0.** ⚠️ **프론트만 → 배포=GitHub Pages push 자동인데, 공개 visible 변경이라 push(=공개 배포) 전 백관장 승인 필요(AGENTS.md 14항).** 달력 셀 색은 라이브 데이터(`class_reservation_summary`) 있어야 렌더돼 오프라인 미검증 — 푸시 후 실화면 확인 권장. 판단 포인트(필요 시 조정 1줄): eyebrow/강조 카드를 중립 `#F6F8FB`로 갈지(규칙4·현 적용) vs 연블루 틴트 유지할지. **가독성 보정(2026-06-30, 백관장 피드백 "선이 흐릿"):** `cream-50`을 순백→**연한 쿨톤 `#F1F4F9`**로 되돌림(원래 `cream-50`=움푹한 배경 토큰, 흰 카드·달력 셀이 그 위에 떠 보이는 구조인데 순백으로 만드니 흰 위 흰이 돼 대비 소실). `ink-200` 테두리 `#E4E6EA→#D3DAE3`(카드마다 `/50~/70` 투명도로 써서 토큰을 진하게 하면 모든 선이 또렷). 달력 셀 ring `/60→/80`. 브라우저로 달력·FAQ·NEXT CLASS·히어로 재확인 완료. email은 인라인 hex라 캔버스 흰/카드 흰/버튼 흰글씨 구분 위험 있어 **색상 스왑까지만**(가독성 톤 미적용) — 필요 시 별도 패스.
>
> 🆕 **운영자 결제 안내 리마인더 D-7(2026-06-26)**: 수업 **7일 전 KST 09시**, 미결제 신청자 있는 수업에 대해 백관장에게 "결제 안내 보낼 시점" 문자 1통(까먹음 방지). 매일 **pg_cron**이 신규 Edge Function **`payment-reminder`**를 내부 호출(`Bearer service_role`) → 오늘+7일(`targetDateKst`)·`payment_reminder_sent_at is null`·**미결제 활성 신청자**(`reservation_status in applied/waitlisted/payment_target` AND `payment_status != paid`)≥1 수업을 찾아 `solapi-reservations`의 **`admin_payment_reminder`** 템플릿(`{class_label}`/`{count}`)으로 발송 + `payment_reminder_sent_at=now()` 기록(**멱등**). 신규 컬럼 `classes.payment_reminder_sent_at`, `admin-reservations.updateClass`는 **날짜 변경 시 이 이력 초기화**(새 날짜 D-7 재발송). 서브에이전트-드리븐 Task 1~4 + 최종 리뷰(Important#1 결제완료 `paid` 집계 제외 수정). 계약 테스트 20개 PASS. 설계 `docs/superpowers/specs/2026-06-26-payment-reminder-design.md`, 계획 `docs/superpowers/plans/2026-06-26-payment-reminder.md`. **✅ 배포 완료(2026-06-26):** ① DB 컬럼 추가 ② `ADMIN_PHONE` 시크릿 설정(01054167694) ③ 함수 3개(payment-reminder/solapi-reservations/admin-reservations) 배포 ④ pg_cron `payment-reminder-daily`(`0 0 * * *`=KST 09시) 등록 완료(jobid 1). **남은 것: 실사용 중 첫 발송 모니터(백관장이 "안 오면 알려줄게"로 보류 — 능동 테스트 안 함).** 안 올 때 진단 순서: ① `select * from cron.job where jobname='payment-reminder-daily';`(등록 확인) ② `select * from cron.job_run_details order by start_time desc limit 5;`(실행·http 결과) ③ 대상일(오늘+7) 수업에 미결제 신청자(`reservation_status in applied/waitlisted/payment_target` AND `payment_status!=paid`)≥1 & `payment_reminder_sent_at is null`인지 ④ `ADMIN_PHONE`·`SOLAPI_*` 시크릿 설정 확인 ⑤ payment-reminder/solapi 함수 로그(대시보드 Functions). 흔한 원인: 대상일 수업 0건/신청 0명, 이미 발송돼 `payment_reminder_sent_at` 찍힘, Solapi 잔액·발신번호 미등록. ⚠️ **service_role 키가 cron 등록 스크린샷으로 채팅 노출됨 — 필요 시 JWT secret 회전(회전하면 cron 재등록 + 프론트 anon 키 갱신·재배포 필요).** **⚠️ 운영 주의:** Solapi 장애일엔 그날 D-7 리마인더가 영구 누락(멱등 우선 설계 — 발송 실패해도 이력 기록), D-7 당일 신청 0명이면 미발송(그 뒤 신청은 관리자 화면으로 확인). `ADMIN_PHONE` 미설정 시 기능 off(안전).

> 🆕 **대기 칸 '만석' 명시(2026-06-24)**: 고객 캘린더 대기 상태 칸이 "대기 가능"만 보여 자리가 찼다는 게 안 와닿는다는 피드백 → 첫 줄에 **"만석"**(굵게, `text-ink-700`), 둘째 줄 **"대기 가능"**. 기존 "N명 대기중" 표현 제거(인원 N을 붙이면 "N명까지 대기 가능"으로 오해돼 숫자 미표기). `classAnchorHtml` 대기 분기 수정. **대기 인원은 칸이 아니라 신청 모달에서 안내** — 대기 칸 버튼에 `data-waiting`(= `max(0, waitlist−available)`)을 싣고, 클릭 시 모달의 `#waitlist-notice`에 **"현재 N명 대기중입니다. 신청하시면 대기 명단에 등록됩니다."** 표시(waiting>0일 때만, 예약가능/0이면 숨김). 프론트만, 배포 불필요·Pages 자동. 계약 테스트 18개 PASS.

> 🆕 **고객 캘린더 칸 상태 단순화(2026-06-22)**: `index.html` 공개 캘린더 날짜 칸이 "가능 N / 대기 N"을 동시에 보여줘 헷갈린다는 피드백 반영 → 칸당 **상태 한 줄만** 표시. **"예약 가능 / N자리 남음"**(brand 색) 또는 **"대기 가능 / N명 대기중"**(회색), 시간 배지는 그대로. ⚠️ 핵심: 예약가능 판정은 단순 `available>0`이 아니라 **`available_count − waitlist_count > 0`**(미확정 빈 자리보다 이미 줄 선 신청 인원이 많거나 같으면 선착순 마감 → '대기 가능'). 뷰의 `available_count`는 `capacity−확정(결제)`이고 `waitlist_count`는 신청(미확정) 인원이라, "가능 6·대기 11"처럼 빈 자리보다 대기가 많은 상태를 '예약 가능'으로 쓰면 안 됨. "N자리 남음"의 N도 `openSeats=available−waitlist`(신규 신청자가 실제로 잡을 수 있는 자리). **"N명 대기중"의 N은 `max(0, waitlist−available)`** — 선착순으로 빈 자리에 들어간 인원은 빼고 자리 초과분만 표기(예: 가능6·신청11 → 5명 대기중). `classAnchorHtml` 분기만 수정(프론트만, 배포 불필요·Pages 자동). 계약 테스트 18개 PASS.

> 🆕 **공휴일 표시(2026-06-21)**: 고객·관리자 캘린더 날짜 칸에 **한국 공휴일(대체공휴일 포함)·일요일을 빨강 + 공휴일명**으로 표시. 신규 `holidays.js`(전역 `window.KR_HOLIDAYS` 맵 + `window.holidayName(dateStr)`)를 양쪽 페이지가 `<script src="holidays.js">`로 로드(캘린더 인라인 스크립트보다 먼저). `index.html`(`dayCellHtml`)·`admin.html`(`renderAdminCalendar`)에서 `holidayName` 참조해 날짜색·공휴일명 렌더, 요일헤더 일요일 빨강. 데이터는 **2026~2027 확정값**(요일·대체공휴일 규칙 교차검증 완료 — 대체 10개 전부 평일, 음력 설날/추석/부처님오신날 정확). **공휴일에도 수업 등록 그대로**(표시 전용, 등록 로직 무변경). 서브에이전트-드리븐 Task 1~4 + 최종 리뷰(데이터 40개 교차검증 ✅). **백엔드·DB 무변경 → push만으로 반영, 배포 불필요.** 계약 테스트 18개 PASS. 설계 `docs/superpowers/specs/2026-06-21-holiday-display-design.md`, 계획 `docs/superpowers/plans/2026-06-21-holiday-display.md`. ⚠️ **운영 메모: 2028년부터는 매년 `holidays.js`에 새 연도 공휴일을 추가**해야 그 해 공휴일이 표시됨(없으면 가드로 안전하게 미표시).

> 🆕 **예약 오픈 일시 + 달력 미리보기(2026-06-21)**: 수업 등록 시 **예약 오픈 일시(`open_at`)** 지정 → 그 전엔 예약 차단(비공개), 시각 지나면 **자동 오픈**(별도 cron 없이 뷰·RLS의 `now()` 비교). 오픈 전이라도 **`preview_before_open=true`** 면 고객 달력에 **'예약 오픈 예정'**(시간 + `M/D 오픈`, 인원 숨김, 클릭 시 "○월 ○일 ○시…" 안내) 표시. `is_public`은 "무조건 숨김" 마스터 스위치로 유지(역할 분리). 서브에이전트-드리븐 Task 1~6, 스펙·품질 리뷰 통과, 계약 테스트 17개 PASS. 변경: `classes` 컬럼 2개(`open_at timestamptz`·`preview_before_open boolean`)+뷰 `class_reservation_summary`(`is_open` 계산·노출조건 `open_at is null or <= now() or preview`)+RLS, `submit-reservation`(오픈 전 거부 "아직 예약이 시작되지 않은 수업입니다"), `admin-reservations`(필드 저장/조회, `open_at` 빈값→null 명시 지우기), `admin.html`(모달 `datetime-local`+미리보기 체크박스+KST↔ISO 변환 `openAtToInput`/`inputToOpenAtIso`, 관리자 캘린더 '오픈예정' 배지), `index.html`(예정 칸 분기 `data-preview-open`·클릭 안내·`renderNextClassInfo`는 오픈분만). 설계 `docs/superpowers/specs/2026-06-19-class-open-schedule-design.md`, 계획 `docs/superpowers/plans/2026-06-19-class-open-schedule.md`. **✅ 배포 완료(2026-06-21):** ① DB SQL 적용 — `alter add column` 2개 + 뷰 `class_reservation_summary` 재생성(새 컬럼 `open_at`/`preview_before_open`/`is_open`은 **맨 끝**에 — `create or replace view`가 기존 컬럼 순서를 못 바꿔 중간 삽입 시 `cannot change name of view column` 에러나므로 끝에 추가) + RLS 재생성. ② Edge Function `submit-reservation`·`admin-reservations` 배포(사용자 보고 기준). ③ 정적 페이지 Pages 자동. **남은 것: 백관장이 관리자에서 미래 `open_at`+미리보기 ON 테스트 수업 등록해 고객 달력 '예약 오픈 예정' 표시·클릭 안내·오픈시각 후 자동 전환을 실제 검증.**

> 🆕 **결제 안내 문자 "바로 결제" 유도 + 일괄승인 confirm 문구 정리(2026-06-20)**: ① `solapi-reservations`의 `payment 안내` 템플릿을 결제 즉시 유도형으로 재작성 — 결제 링크를 본문 맨 위(첫 행동 유도 바로 뒤)로 올리고 "결제 완료 순으로 자리가 확정되니 지금 바로 결제해 주세요"로 시작, 마지막 줄에 "그러니 잊지 말고 지금 바로 결제해 주세요" 추가. 24시간은 마감 백업으로 남기되 톤 낮춤(기존엔 "24시간 이내"가 맨 아래라 미룸을 허락하는 구조였음). ② `admin.html` 선착순 일괄 승인 confirm 문구를 "선착순으로 들어온 N명을 '결제 안내 대상'으로 일괄 승인합니다. (정원을 못 채운 나머지는 자동 대기)"로 단순화 — 기존 "초과분은 자동 대기"가 취소자까지 포함하는 것처럼 읽히던 모호함 제거. 동작 변경 없음(bulkApprove는 원래 cancelled/no_show/confirmed 제외). 계약 테스트 16개 PASS. **⚠️ 남은 것: `supabase functions deploy solapi-reservations --no-verify-jwt` 배포 — 이번 세션은 CLI 401(미로그인)로 미배포, 로그인 후 재시도 필요.**

> 🐛 **모바일 캘린더 배지 깨짐 수정(2026-06-19)**: `index.html` 공개 캘린더 날짜 칸이 모바일에서 폭 40px로 좁아 `13:00~16:00` 한 줄 시간 배지가 칸 밖으로 삐져나가던 문제 수정. ① 시간 표기를 모바일만 2줄(`<br class="sm:hidden">`)로, 폰트 `9px`(sm 이상 `11px` 한 줄 복원) ② 카운트 라벨 "예약 가능 N"→"가능 N"·`whitespace-nowrap` ③ 셀/배지에 `overflow-hidden`(옆칸 침범 방지) ④ 모바일 그리드 `gap 6px→4px`, 캘린더 컨테이너 패딩 `p-4→p-2`로 칸 폭 40→44px 확보. Playwright로 iPhone 폭(390)·데스크탑(1100) 양쪽 렌더 검증(모바일 2줄 칸내 정렬, 데스크탑 한 줄 회귀 없음). 프론트만 변경(배포 불필요). 계약 테스트 16개 PASS.

> 🆕 **예약 안내 섹션 개선(2026-06-18)**: `index.html` '예약 안내'를 카드 나열→**세로 타임라인(번호 1~6 + 연결선)**으로 바꾸고 위치를 **캘린더(`#schedule`) 위로** 이동(흐름: hero→예약 안내→예약 가능 일정). 3단계 결제 문구를 "**예약이 확정되면, 문자로 결제 안내를 보내드립니다.**"로 변경(+ "결제까지 완료되어야 수업 자리가 확정됩니다." 유지 — 예약 확정 vs 결제=자리확정 구분, 테스트 의존). 타임라인 아래 **"대기로 신청된 경우"** 안내 박스 추가(정원 차면 대기·자리 나면 순서대로 결제 안내·대기 중 결제 없음, 개인 대기순번은 비노출). 프론트만 변경(배포 불필요, Pages 자동). 계약 테스트 16개 PASS.

> ✅ **관리자 월간 캘린더 UI — 구현 완료(2026-06-15)**: 설계 `docs/superpowers/specs/2026-06-15-admin-calendar-ui-design.md`, 계획 `docs/superpowers/plans/2026-06-15-admin-calendar-ui.md`. 서브에이전트-드리븐으로 Task 1~6 구현, 스펙·코드 품질 리뷰 통과, 계약 테스트 16개 PASS. `admin.html`만 변경(백엔드·DB 무변경). 요지: "일정 선택" 드롭다운 제거→선택 상태를 모듈 변수 `currentClassId`로 관리(`selectedClassId()`→`currentClassId`), 등록/수정 공용 모달(`openClassModal(mode, classItem)`, 인라인 폼 대체), 월간 캘린더(`renderAdminCalendar`/`admin-cal-body` — 칸에 시간·상태·신청현황 배지, 빈날짜 클릭→등록 모달/수업 배지 클릭→수정·삭제 모달, ‹/오늘/›/＋새수업), 목록 표 행 클릭→수업 선택+하이라이트(`setSelectedClass`/`data-select-class`). **남은 것: 라이브 배포는 GitHub Pages 자동(push 시) — 백관장 실제 화면 확인.**

> 🆕 **관리자 결제 24시간 경과 배지(2026-06-16)**: `admin.html` 예약 표 '결제' 셀에 결제 안내/여석 안내 문자(`payment 안내`/`seat_opened`, status=sent) 발송 후 24시간 지났는데 미결제(payment_target/sent, paid·cancelled·confirmed 아님)면 `Nh 경과` amber 배지 표시(`paymentSentAt`/`paymentOverdueHtml`, message_logs 기반). **자동 마감 아님** — 표시만, 마감은 기존 '미결제 마감' 버튼으로 백관장 수동. `payment_expired` 문자는 원래부터 수동(버튼)이라 변경 없음. 프론트만 변경(배포 불필요, Pages 자동).

> 🆕 **결제 링크 발송 시점 안내(2026-06-16)**: "결제 링크는 수업 약 1주일 전에 문자로 발송" 문구를 조회 페이지(applied/waitlisted 안내)와 안내 문자(`reservation_success`/`reservation_waitlist`) 양쪽에 반영. **lookup-reservation/cancel-reservation/solapi-reservations 3개 라이브 배포 완료**(npx supabase, SUPABASE_ACCESS_TOKEN 환경변수). 조회 페이지 4개 상태 렌더 캡처로 확인. ⚠️ 사용한 PAT 채팅 노출 — 대시보드에서 폐기 권장.

> 🆕 **내 예약 조회 보강 + 본인 취소 — 코드 완료(2026-06-16, 배포 완료)**: `lookup.html`이 상태별 안내 문구(다음 행동), 신청일, 확정자 준비물, **예약 취소 버튼**(결제 전 상태만)을 표시. `lookup-reservation`이 `reservation_id`/`status_key`/`created_at`/`cancellable`를 추가 반환(`customerStatusLabel`은 key 기반으로 리팩터, 라벨 동일). 신규 공개 함수 **`cancel-reservation`**: 이름+전화+예약ID 일치 & 결제 완료 전(`payment_status != paid`, status in applied/waitlisted/payment_target)일 때만 PATCH로 cancelled 처리 + 취소 문자(베스트에포트). 결제 링크는 페이지에 버튼 없이 "승인 후 문자로 발송"만 안내. 예약 완료 문자(`reservation_success`/`reservation_waitlist`)도 "신청 승인 후 결제 링크를 문자로 보내드립니다"로 수정. `config.toml`에 `cancel-reservation verify_jwt=false` 추가. **DB 무변경**(cancelled 상태 기존). **남은 것: `supabase functions deploy cancel-reservation/lookup-reservation/solapi-reservations --no-verify-jwt` 배포 + 실문자 확인.** 계약 테스트 16개 PASS.

**마지막 갱신:** 2026-07-19 (**중복 등록 취소대상 표시** — 다른 수업 결제 확정자의 남은 신청에 배지+전용 필터, 프론트만. 같은 날: **문자 없이 취소 처리** — 취소 모달에 '문자 없이 처리' 버튼, 서버 silent 지원(정리 작업은 수행), admin-reservations 배포. 같은 날: **전체 보기 일괄 처리 허용** — 문자 나가는 액션은 같은 수업 선택만, 미리보기는 선택자 수업 기준. 같은 날: **구분 배지 색·라벨 통일** — 두 보기 공통 globalBadgeHtml 하나로(선착순=청록·결제 안내 중=주황 구분). 같은 날: **전체 수강생 보기+상태 필터** — 전 수업 신청자 한 표(수업일 컬럼)+선착순/결제 안내 중/확정/대기/취소 필터, 전체 보기에선 일괄 처리 바 숨김. 프론트만. 같은 날: **환불 규정+환불 처리 버튼** — FAQ 환불 규정(시작 전 전액 환불), 벌크 '환불 처리'(cancelled+refunded+payment_refunded 문자), 함수 2개 배포. 같은 날: **수강생 검색** — 전체 수업 대상 이름/전화 검색, 결과 클릭 시 해당 수업 이동+자동 선택. 프론트만, 계약 23 PASS. 같은 날: **즉시 결제 전환 + 결제 기한 리마인드** — 정원 내 신청은 접수 즉시 payment_target+결제 링크 문자(결제 완료 순 확정), 대기는 무료 유지. 기한 절반 시점 리마인드 예약 발송+결제/취소 시 자동 취소, 본인 취소의 예약 문자 취소 갭도 해소. index 문구 정합화. 함수 4개 배포+preview 실검증, 계약 23 PASS. 8월 기존 신청자(applied) 결제 요청은 백관장이 기존 승인 버튼으로 직접. 같은 날: **예약 거부(차단) + 결제 기한 설정** — blocked_phones/app_settings 테이블 신설(운영 DB 적용), 차단 번호 신규 신청 403, 벌크 '예약 거부' 버튼+차단됨 배지 해제, payment_deadline_hours 설정이 문자 치환자·'Nh 경과' 배지(구분 칸으로 이동) 기준에 공용 적용, 저장된 수정 템플릿의 24시간 리터럴 마이그레이션, 함수 3개 배포+실측 검증 완료, 계약 22 PASS. 같은 날: **문자 발송 현황에 발송 시각 표기** — sent 행에 "· 7/18 14:32 발송"(KST, 최근 시각) 추가. admin-reservations select에 sent_at 포함(재배포 완료) — 이걸로 '24h 경과' 배지 미동작 버그도 함께 수정. 계약 21 PASS. 이전: 2026-07-18 (**문자 발송 전 확인/수정 모달** — 관리자 수동 문자 전 경로에 발송 전 본문 미리보기+수정+확인 모달 추가. `solapi-reservations` preview/overrideText, `admin-reservations` previewMessage 액션+messageText 전달, `admin.html` sms-modal. 두 함수 배포 완료+401 스모크 통과, 계약 21 PASS. 이전: 2026-07-11 (**문자 발송현황 예약시각 UTC 표시 버그 수정** — `schedLabel`이 timestamptz의 UTC 반환값을 문자열 그대로 표기해 "7/11 07:00"처럼 보이던 것을 KST(+9h) 변환 표기로 수정. "지남" 판정은 원래 정확(절대시각 비교). admin.html만, push로 Pages 자동. node 5케이스 + 계약 테스트 PASS. 이전: 2026-07-07 **'선착순 확보(결제 링크 X)' 버튼 신설** — 수업 한참 남은 대기자에게 결제 링크 없이 자리만 확보 통보. `payment_target` 재사용(+`payment_status='pending'`)으로 DB 마이그레이션 없음, 새 `seat_secured` 문자, admin 벌크 버튼 + 배지 "선착순 확보 N"(sent=결제 안내 중과 구분). 계약 20 PASS + node 검증. **✅ 배포 완료(2026-07-07): 6개 함수 전부 npx supabase로 배포**(solapi/admin-reservations + 그동안 밀렸던 submit-reservation·lookup-reservation·cancel-reservation·payment-reminder), 인증 게이트 스모크(401/400) 전부 통과 → 라이브=저장소 일치. 이후 D-7에 기존 '결제 안내 대상으로 지정'으로 실제 링크 발송. 이전: 2026-07-06 **달력에 오늘 날짜 표기** — index/admin 월간 달력 오늘 칸을 파란 pill+링으로, index는 빨강→파랑(공휴일과 구분) 변경·admin은 신규. 프론트만, push로 Pages 자동. 계약 20 PASS. 이전: 2026-07-04 **결제 안내 문자에도 '원치 않으면 회신' 안내 추가**(solapi payment 안내 템플릿, 여석 안내와 동일 톤). 계약 20 PASS. 배포 대기: solapi-reservations. 이전: **대기 접수 문자에 대기 순위 표기** — reservation_waitlist에 "대기 N순위로 접수"(신청 시점, 취소 시 앞당겨짐), submit-reservation이 순위 계산해 solapi로 전달, AGENTS.md 순번 비노출 정책의 문자 예외 명시. 계약 20 PASS. 배포 대기: submit-reservation + solapi-reservations. 이전: **중복 신청 정책 재조정 '대기 허용·선착순만 번호당 1건' + 여석 문자 회신 안내** — submit-reservation이 신청 시점 선착순/대기 판정(대기는 waitlisted 저장·검사 skip, 선착순은 blocker 있으면 409/같은수업·다른수업 문구 분리), schema 인덱스 `(class_id,phone)` 원복(운영 DB 무변경·1차 인덱스교체 SQL 취소), admin 상단 배너는 제거하고 행 '중복 N건' 배지를 버튼화(클릭 시 날짜별 순위 alert), solapi 여석문자에 "원치 않으면 회신"(발신·회신번호=01054167694 확인). 계약 20개 PASS + node 검증. **배포 대기: submit-reservation + solapi-reservations 재배포.** 이전 같은 날: **중복 차단 '번호 전체'로 1차 구현**(→ 위처럼 재조정됨). 이전: 2026-06-30 **색상 테마 → 브랜드 블루 전환** — 공개 페이지 색상만 백관장 홈페이지 톤(블루)으로 교체, 레이아웃·텍스트·동작 무변경. Tailwind config 팔레트 토큰 일괄 스왑(주황→`#0A4DB0`/`#083C8C`, 웜크림→흰색·`#F6F8FB`) + 다크 섹션 포인트 `#6AA4FF` + 달력 규칙6(예약가능 솔리드 블루·대기 다크칩) + amber 박스 중립화. `index`/`checkout`/`lookup`/`email` 전부. 계약 테스트 20개 PASS, 잔여 주황 hex 0. **로컬 커밋만 — push(=공개 배포)는 백관장 승인 후.** 이전: 2026-06-27 (**복습 영상 안내 문자 시스템 발송 + 수업 전 문자 주차 안내** — ① `class_reminder`(수업 전날) 끝에 "별도 주차 공간이 없으니 대중교통을 이용해 주시기 바랍니다" 추가. ② 복습 영상 문자를 '백관장 수동 발송'→**관리자 화면에서 발송**으로 전환: 신규 `review_video` 메시지 타입/템플릿(`{video_url}` 치환, 끝에 "고생하셨습니다!"), `admin-reservations.resendMessage`가 `videoUrl`을 받아 발송(RESENDABLE 화이트리스트 추가, 링크 없으면 에러), `admin.html` 문자 발송현황 '복습 영상 안내 문자' 행에 **항상 뜨는 `영상 링크 발송 N명` 버튼**(클릭→`prompt`로 링크 입력·http 검증→`resendMessage` 호출). 발송 대상은 복습 자료(`review_material`) 수신자=결제완료자 집계를 그대로 재사용(별도 대상 판정 로직 없음). 수업마다 링크가 달라 classes 저장 없이 매번 입력. 계약 테스트 20개 PASS(`assertNotIn review_video`→`assertIn`으로, '수동 발송'→'data-send-video'로 갱신). **⚠️ 배포 대기: `solapi-reservations`+`admin-reservations` 두 함수 배포 필요**(admin.html은 push로 Pages 자동). 직전 배포 시도가 프로젝트 폴더 **밖**에서 실행돼 `index.ts no such file`로 실패 — 반드시 프로젝트 루트(`cd "…/12. 케틀벨 원데이 수업 상세페이지"`)에서 `supabase functions deploy …`. `Docker is not running`은 원격 빌드라 무시 가능 경고.) 이전: 2026-06-27 (**문자 발송현황에 예약 시각 표기** — 예약 문자(class_reminder/review_material)는 Solapi가 예약시각에 자동 발송하지만 우리 DB로 콜백/웹훅이 없어 현황판에 영원히 'scheduled(예약)'로 남는다(콜백 추가 안 함 — Solapi 인바운드 웹훅 설정·검증 비용 大). 그래서 "예약만 되어있고 안 나갔다"는 건 *표시상*일 뿐 실제 미발송 아님. 해결: `admin-reservations` list가 `scheduled_at`도 내려주고, admin.html 현황판이 예약 시각을 "· 6/26 18:00 발송 예정 / 예약시각 지남(발송 완료 추정)"으로 표기(`schedLabel`). 같은 수업이면 전원 동일 시각이라 행당 1회 표기. 계약 테스트 20개 PASS. **✅ 배포 완료(2026-06-27): `admin-reservations`+`solapi-reservations` 두 함수 배포**(admin.html은 push로 GitHub Pages 자동 반영). solapi엔 class_reminder 주차 안내 문구도 함께 반영됨. 실제 미발송 의심되면 Solapi 콘솔에서 해당 예약 그룹 상태(잔액/발신번호) 확인. 이전: 2026-06-26 (**운영자 결제 안내 리마인더 D-7 — 구현 완료, 배포 대기** — 위 🆕 블록 참조. payment_reminder_sent_at 컬럼 + solapi admin_payment_reminder 템플릿 + 신규 payment-reminder Edge Function + admin 날짜변경 초기화, 서브에이전트-드리븐 Task 1~4 + 최종 리뷰(paid 집계 제외 수정), 계약 테스트 20개 PASS. **DB 컬럼 + 함수 3개 배포 + ADMIN_PHONE 시크릿 + pg_cron 등록 필요.** 이전: 2026-06-24~22 고객 캘린더 대기/상태 문구 개선. 이전: 2026-06-21 (**공휴일 표시(대체공휴일 포함) — 구현 완료** — 위 🆕 블록 참조. holidays.js 신규 + index·admin 캘린더 공휴일·일요일 빨강+이름, 2026~2027 데이터 40개 교차검증, 서브에이전트-드리븐 Task 1~4 + 최종 리뷰 ✅, 계약 테스트 18개 PASS, 정적 파일이라 push만으로 반영. 이전 같은 날: **예약 오픈 일시 + 달력 미리보기 — 구현·배포 완료** — 위 🆕 블록 참조. classes 컬럼·뷰·RLS·submit-reservation·admin-reservations·admin.html·index.html 전 구간, 서브에이전트-드리븐 Task 1~6 + 최종 리뷰(Critical escapeHtml 1건 수정), 계약 테스트 17개 PASS. DB SQL·Edge Function 2개 배포 완료, 백관장 테스트 수업 검증만 남음. 이전: 2026-06-20 (**결제 안내 문자 "바로 결제" 유도 + 일괄승인 confirm 문구 정리** — 위 참조. ⚠️ solapi-reservations 배포 미완(CLI 미로그인). 이전: 2026-06-19 **모바일 캘린더 시간 배지 깨짐 수정** — 위 참조. 이전: 2026-06-18 **예약 안내 섹션 타임라인화 + 캘린더 위로 이동, 결제/대기 문구 보강** — 위 참조. 이전: 2026-06-16 **내 예약 조회 보강 + 본인 취소** — 위 참조. 이전: 2026-06-15 **관리자 월간 캘린더 UI 구현 완료** — 아래 참조. 이전 같은 날: **구글 캘린더 동기화 — 배포·검증 완료**: 수업 생성/수정/삭제 시 `admin-reservations`가 구글 Calendar v3 이벤트를 베스트 에포트로 생성·갱신·삭제(`calendar.ts`, 서비스계정 JWT(RS256)→OAuth→REST Deno 직접 호출). `classes.google_event_id` 컬럼·시크릿 3개(`GOOGLE_CLIENT_EMAIL`/`GOOGLE_PRIVATE_KEY`/`GOOGLE_CALENDAR_ID`, 근력학교 앱과 동일 서비스계정·캘린더 재사용) 라이브 적용 완료. 이벤트 제목 `[케틀벨 원데이] M월 D일 (요일)`, **파란색 colorId 9**(근력학교 일정과 구분). 기존 수업 일괄 등록 버튼(`backfillCalendar`). **결제완료 예약 있는 수업 삭제 방어**(force 재확인 필요). 실제 캘린더 생성/색상 검증 완료(아이폰 기본 캘린더는 캘린더색으로 덮어 보일 수 있음 — 구글 캘린더 앱/웹에선 파랑 정상).) (이전: 1차 + 운영 피드백 다수 반영·배포 완료 — 예약 문자 버그 수정(`send-many/detail`+ISO8601), 취소 시 자리 복구(취소·불참 집계 제외), 접수 문자 2분기(정원 내/만석), 본인 예약 조회 페이지(`lookup.html`+`lookup-reservation`), 어드민 기본 시간 13:00~16:00, 현황판 실제 이름 표시, 상단 새로고침 버튼.)

> **공개 직전 체크리스트(미완 추정 — 확인 필요)**: ① 라이브 뷰 SQL 적용(`class_reservation_summary` 취소 제외 — 미적용 시 고객 달력 인원 부정확) ② 테스트 데이터 정리(`delete from message_logs; delete from reservations;`) ③ 노출된 Supabase PAT 폐기 ④ 공개 전 백관장 승인(AGENTS.md 14항 — 운영자 본인 최종 검토).

> 문자 템플릿 정책: `[근력학교]` 접두어 없음(LMS 제목 '케틀벨 원데이 수업'). 결제 마감은 **24시간 고정 문구**("안내 문자를 받은 뒤 24시간 이내")로, 시각 입력 없이 운영. AGENTS.md 9항 톤(여러 줄) 반영. 결제 안내/확정 문자는 수업별 `{장소}`까지 채움.

---

## 1. 한 줄 상태

관리자 수업 일정 등록 → 고객 페이지 동적 달력 노출 → 모달 예약(공개 `submit-reservation` 함수 경유, anon 직접 insert 차단) → 접수/결제안내/여석/확정/리마인드/복습/취소 문자 자동화 → 관리자 선착순·일괄 처리·문자 현황판까지 **구현·배포·검증 완료**. 남은 건 실발송 수동 검증·테스트 데이터 정리·공개 승인뿐.

## 2. 이번까지 완료한 기능

- **관리자 월간 캘린더 UI — 구현 완료(2026-06-15)** (`admin.html` + `tests/test_static_pages.py`, 백엔드·DB 무변경)
  - "일정 선택" 드롭다운(`class-filter`) 제거 → 선택 상태를 모듈 변수 `currentClassId`로 단일화(`selectedClassId()`가 이를 반환). 기본 선택은 가장 가까운 다음 일정.
  - 등록/수정 **공용 모달**(`class-modal`, `openClassModal('create'|'edit', classItem)`): 인라인 등록 폼 대체. 저장은 `createClass`/`updateClass`, 삭제는 결제완료 예약 방어(force 재확인) 그대로. 배경/X/ESC 닫기.
  - **월간 캘린더**(`admin-cal-body`/`renderAdminCalendar`): 날짜 칸에 수업 배지(시간 `13:00~16:00`·상태 라벨·신청현황 `확정 N·가능 M·대기 K`, 상태별 색, 비공개는 흐리게). 빈 날짜 클릭→등록 모달(날짜 프리필), 수업 배지 클릭→수정/삭제 모달. ‹/오늘/›/＋새수업 버튼으로 월 이동·등록.
  - **목록 표 행 선택**: 행 클릭(`data-select-class`)→`setSelectedClass(id)`로 아래 요약·예약표·문자현황 갱신 + 선택 행 하이라이트(`bg-blue-50 ring-2 ring-blue-300`). 관리 버튼(수정/공개토글/삭제)은 기존 동작 유지(버튼 우선 처리로 행 선택과 분리).
  - 서브에이전트-드리븐 구현(Task 1~6) + 스펙/코드 품질 리뷰 통과. 계약 테스트 `test_admin_calendar_ui` 추가, 제거된 `data-class-form` 검사 삭제 — 16개 PASS.
- **구글 캘린더 동기화 — 구현·배포·검증 완료(2026-06-15)** (`admin-reservations/calendar.ts` 신규 + `index.ts` + `admin.html` + `schema.sql`)
  - 수업 등록→이벤트 생성, 수정→갱신, 삭제→제거. 베스트 에포트(시크릿 미설정/캘린더 실패해도 수업 CRUD 정상).
  - Deno에서 서비스계정 JWT(RS256)→OAuth(`oauth2.googleapis.com/token`)→Calendar v3 REST 직접 호출. 근력학교 앱과 **같은 서비스계정·같은 캘린더** 재사용(시크릿 3개 라이브 설정 완료). 토큰 메모리 캐시. private key는 `\n` 복원 후 DER 디코드.
  - `classes.google_event_id` 컬럼으로 수업↔이벤트 연결(라이브 DB 적용 완료). `pickClassFields`가 클라 주입 차단.
  - 이벤트: 제목 `[케틀벨 원데이] M월 D일 (요일)`, 시간 `Asia/Seoul`(자정 넘김 시 종료일 +1), 장소·예약링크, **colorId 9(파랑)**.
  - **일괄 등록 버튼**(`backfillCalendar` 액션): `google_event_id` 없는 기존 수업을 한 번에 캘린더 등록(멱등 — 재실행 안전).
  - **삭제 방어**: 결제완료(confirmed/paid) 예약 있는 수업은 `deleteClass`가 차단, `force:true`(관리자 재확인) 시에만 삭제.
  - 설계/계획: `docs/superpowers/specs/2026-06-14-google-calendar-sync-design.md`, `docs/superpowers/plans/2026-06-14-google-calendar-sync.md`.
- **스키마/테스트 배치 — 코드 완료(2026-06-13, 라이브 DB 미반영)** (`supabase/schema.sql` + `tests/test_static_pages.py`)
  - `anon can create reservation` insert 정책 삭제 — 예약 신청은 submit-reservation(service_role) 경유만 허용.
  - 부분 unique 인덱스 `reservations_active_unique` 추가: `(class_id, phone)` where `reservation_status not in ('cancelled','no_show')` — 같은 수업·같은 번호 활성 신청 중복을 DB 차원에서 차단.
  - 시드 insert 위에 "예시 시드(날짜는 과거일 수 있음, 운영 DB에는 적용하지 말 것)" 주석 추가.
  - 계약 테스트 `test_public_submit_reservation_function` 추가(submit-reservation 검증·프론트 직접 insert 금지·어드민 메모편집·일괄승인 가드·취소 템플릿·내부 인증·스키마 정책/인덱스) — 총 13개 통과.
  - **주의: 라이브 DB에는 아직 미적용.** SQL Editor에서 `drop policy "anon can create reservation" on public.reservations;` + unique index 생성 구문을 실행해야 함(전체 schema.sql 재실행 금지 — 시드 포함).
- **프론트 개편 배치 — 코드 완료(2026-06-13)** (`index.html` + `admin.html` + 계약 테스트)
  - `index.html` 신청 폼: 이메일 입력 삭제, 개인정보 수집·이용 동의 체크박스(필수) 추가. `submitReservationToSupabase()`가 anon REST insert 대신 `functions/v1/submit-reservation` 호출 — 서버의 한글 오류 메시지(`error.serverMessage`)를 그대로 안내, 성공 문구는 "접수 확인 문자를 보내드립니다".
  - 수업 정보 섹션 동적화: `next-class-schedule`/`next-class-availability`/`next-class-place` + `renderNextClassInfo()`가 가장 가까운 다음 일정을 한국어 라벨(`koreanScheduleLabel`)로 표시. 하드코딩 날짜/현황 제거. 다음 일정 없으면 "새 일정 오픈 준비 중".
  - `data/classes.json` 폴백 삭제 — Supabase 실패 시 달력 빈 상태 + "일정을 불러오지 못했습니다. 잠시 후 새로고침해 주세요." 안내.
  - `admin.html`: 이메일 컬럼 제거(colspan 12), 메모 셀이 클릭 → prompt 편집(`admin_memo`만 update — 상태 키 없으니 문자 미발송), 일괄 승인 힌트가 결제 안내 중 인원 차감(`capacity - confirmed - payment_ready`), 문자 현황판에 `reservation_received`(자동화됨)·`reservation_cancelled` 행 추가.
  - 계약 테스트 같은 커밋 수정(이메일 금지·privacy_consent/submit-reservation 요구·classes.json 테스트 삭제 등) — 12개 통과.
- **admin-reservations 동작 개선 — 코드 완료(2026-06-13, 재배포 필요)** (`supabase/functions/admin-reservations/index.ts`)
  - `updateReservation`: 이번 요청이 `reservation_status`/`payment_status`를 실제로 바꿨을 때만 문자 분기 실행(메모만 수정해도 문자가 재발송되던 사고 방지).
  - 취소 처리 시 `reservation_cancelled` 취소 안내 문자 발송 + 예약된 리마인드·복습 취소(미결제 마감은 기존 `payment_expired` 문자 유지).
  - `bulkApprove` 재클릭 가드: 이미 결제 안내를 받은 `payment_target`을 자리 점유로 차감하고 후보에서 제외(중복 문자·초과 승인 방지).
  - `scheduleFollowups`: 발송 시각이 이미 지난 리마인드/복습은 조용히 빠지지 않고 message_logs에 `skipped`로 기록(현황판 노출).
  - `RESENDABLE_TYPES`에 `reservation_received`/`reservation_cancelled` 추가. 함수 간(solapi) 호출은 Bearer service_role 내부 인증이므로 `sendSms`/`notify`/`cancelScheduledFollowups` 체인 전체에서 평문 비밀번호 전달 제거(클라이언트 인증 게이트 `assertAdminPassword`는 유지).
  - `submit-reservation`: 동시 신청 레이스로 unique 인덱스(23505) 위반 시 영어 DB 오류 대신 409 한글 안내("이미 이 수업에 신청되어 있습니다…")로 응답.
  - `submit-reservation`의 `isPastClassKst`: 날짜/시각 파싱 실패 시 '종료된 수업' 오인 차단 대신 명시적 오류 throw.
- **admin-auth 죽은 코드 제거(2026-06-13)** — `supabase/functions/admin-auth/` 삭제(1시간 토큰 발급하나 어디서도 미사용 — admin.html은 매 요청 비밀번호 전송 방식). admin.html의 `authEndpoint` 참조 제거, 계약 테스트 2건 admin-reservations 기준으로 수정. 라이브 함수 삭제는 선택: `supabase functions delete admin-auth --project-ref vjoxzbxcylqyhxezxiuj`. (CLAUDE.md의 admin-auth 언급 정리는 마지막 문서 배치 담당.)
- **공개 신청 엔드포인트 `submit-reservation` 신설 — 코드 완료(2026-06-13, 배포 전)** (`supabase/functions/submit-reservation/index.ts`)
  - 검증(이름·010 11자리·개인정보 동의·class_id) → 신청 가능 수업 확인(공개+숨김 아님+시작 전) → 같은 수업 활성 신청 중복 차단(409) → service_role insert → 접수 확인 문자(베스트 에포트, message_logs 기록).
  - anon 직접 insert를 대체하는 작업의 **서버 측 선행 단계**. index.html 프론트 연동·anon insert 정책 제거·함수 배포는 **다음 배치** 담당.
  - `solapi-reservations` 확장: `reservation_cancelled` 취소 안내 템플릿 추가 + "Authorization: Bearer <service_role key>" 내부 호출이면 관리자 비밀번호 없이 인증 통과(timing-safe 비교).
  - `supabase/config.toml`: `[functions.submit-reservation] verify_jwt=false` 추가, `[functions.admin-auth]` 블록 제거(admin-auth 함수 자체 삭제는 다음 배치).
- **문자 발송 현황판 + 재발송 — 구현·배포 완료(2026-06-07)** (`admin.html` + `admin-reservations`)
  - admin `list`가 `message_logs`를 함께 내려주고, 선택 일정 기준으로 종류별 발송/예약/미발송을 집계해 표시.
  - 전원 성공 시 "전체 발송 완료(N명)"/"전체 예약 완료(N명)", 일부 누락 시 "발송 완료(제외: 이름…)" + [재발송 K명] 버튼.
  - 재발송은 `resendMessage` 액션(즉시형 즉시발송 / 예약형 재예약, 과거 시각이면 skip). 분모=보낸 시도 기준.
  - ✅ admin-reservations 재배포 완료, resendMessage 인증 게이트 스모크 통과(틀린 비번 → 401).
- **문자 자동화 마무리 — 구현·배포 완료(실발송 검증 전)** (`admin.html` + `admin-reservations` + `solapi-reservations`)
  - ✅ 두 함수 `--no-verify-jwt` 재배포 완료(2026-06-07). 인증 게이트 스모크 통과(틀린 비번 → 401). 취소 실패는 message_logs `cancel_failed`로 기록.
  - **여석 안내(수동)**: 관리자 '여석 안내' 일괄 액션 → 대기자에게 여석 안내 문자 발송 + 결제 안내 대상(payment_target/sent) 전환.
  - **수업 전 리마인드 + 수업 후 복습 자료(자동 예약발송)**: '결제 완료 처리' 시 Solapi 예약 발송 등록 — 리마인드=수업 전날 18:00(KST), 복습=수업 종료 시각(KST). cron 없이 Solapi `scheduledDate` 사용.
  - **취소 연동**: '취소 처리'/'미결제 마감' 시 해당 예약의 예약된 리마인드·복습 문자를 Solapi에서 취소(`DELETE .../groups/{groupId}/schedule`).
  - 중복 방지(message_logs status in sent/scheduled 체크), 과거 시각 가드, groupId를 message_logs.provider_message_id에 저장.
- **관리자 수업 일정 CRUD** (`admin.html` + `admin-reservations` Edge Function)
  - 등록/수정/공개토글/삭제. `createClass`/`updateClass`/`deleteClass` 액션.
  - 목록은 raw `classes` 테이블을 읽어 숨김·비공개 수업까지 관리자에 표시.
- **고객 페이지 동적 달력** (`index.html`)
  - 하드코딩 제거, Supabase `class_reservation_summary`로 어느 달이든 동적 생성. (폴백 `data/classes.json`은 2026-06-13 삭제 — 실패 시 안내 문구.)
  - 관리자가 등록하면 자동 노출.
- **예약 신청 모달** (`index.html`)
  - 달력 날짜 클릭 → 스크롤 대신 모달 팝업(배경/X/ESC 닫기, 성공 시 자동 닫힘). 달력 칸은 `<button>`.
- **관리자 일괄 처리** (`admin.html`)
  - 행 앞 체크박스 + 전체선택 → 상단 액션바에서 결제 안내 대상 지정/결제 완료/대기/취소 일괄 적용.
  - `선착순 일괄 승인`(`bulkApprove` 액션): 예약 가능 인원 − 확정 인원 만큼 선착순 결제 안내 대상 지정, 초과분 자동 대기(waitlist_order 부여).
  - `구분` 컬럼: 선착순/대기/확정/취소 배지(신청 시간순).
- **문구/UI 정리**
  - 상세/결제/이메일 페이지 AI 티 윤문 + 줄바꿈(Humanize KR 가이드 적용).
  - 어드민 화면에서 기술 용어 제거(Supabase/ADMIN_PASSWORD_HASH 등은 개발자용 JS 주석으로만).
  - 예약 현황 표 줄바꿈 방지(whitespace-nowrap) + text-xs.
- **보안**: `class_reservation_summary` 뷰에 `is_public = true` 필터 추가(비공개 서버단 차단).
- **지난 수업 처리(프론트 계산, 시작 시각 기준)** (`index.html` + `admin.html`)
  - 공통 헬퍼 `isPastClass(c)`: `class_date`+`start_time`이 현재 시각보다 과거면 종료로 판정(로컬/KST 기준). admin엔 `upcomingClasses()`(시작 전만 가까운 순) 헬퍼도 추가.
  - 고객 달력(`classAnchorHtml`): 지난 수업도 **달력에 그대로 표시하되 '종료'로만 표기**(회색·opacity, `data-reservation-date` 없는 `<div>`라 클릭/예약 불가). 아예 숨기지 않음.
  - 관리자 '일정 선택' 드롭다운: `예정된 수업`(가까운 순) / `종료된 수업`(최근 순) optgroup으로 나눠 **둘 다 선택 가능**(과거 수업도 나중에 조회 가능). 기본 선택은 가장 가까운 **다음 일정**(`renderAdminData`).
  - 관리자 '수업 일정 등록/관리' 표는 종료 수업도 표시(행 muted + `종료` 배지, 기록·삭제용).
  - DB status 변경/cron 없음(YAGNI).
- **고객 달력 수업 시간 표기** (`index.html`): 칸에 `시작~종료`(예: `13:00~16:00`) 표시. `normalizeClass`에 `end_time` 추가 + `timeRange()` 헬퍼(예약가능/대기/종료 칸 공통). end_time 없으면 시작 시각만.
- **자동 문자(Solapi) — 코드 완료** (`solapi-reservations` + `admin-reservations`)
  - `solapi-reservations`: 실제 HMAC-SHA256 발송 구현(스텁 아님). 관리자 비밀번호로 보호(요금 폭탄 방지). 키 없으면 안전 skip.
  - `admin-reservations`: 상태 전환 시 서버사이드 자동 발송 — '결제 안내 대상 지정/일괄 승인' → 결제 안내 문자, '결제 완료' → 확정 문자. `message_logs` 기록(베스트 에포트).
  - ✅ **시크릿 설정·배포 완료(2026-06-05).** 두 함수 `--no-verify-jwt` 라이브. 인증 게이트 스모크 통과. 실제 발송(실문자)만 운영자 번호로 최종 확인하면 끝.

## 3. 배포 / 운영 상태

- **GitHub Pages** (main 브랜치 자동 배포). Vercel 등 불필요(순수 정적). canonical `https://baekstrong.github.io/productdetailpage/`.
- **Edge Function `admin-reservations`**: 최신 코드로 배포됨. **반드시 `--no-verify-jwt`로 배포**(게이트웨이 JWT 검증 끄기 — publishable 키는 JWT 아님, 자체 비밀번호 인증). `supabase/config.toml`에 세 함수 `verify_jwt=false` 명시됨.
- **DB 뷰**: `is_public` 필터 버전이 라이브에 적용 완료(SQL Editor에서 수동 실행함).
- **DB 스키마**: `supabase/schema.sql` 기준. 테이블 변경 없음(현재 기능엔 스키마 변경 불필요).
- **테스트**: `python3 -m unittest tests.test_static_pages` — 16개 통과(관리자 캘린더 UI 계약 테스트 추가).

### 환경 메모 (다음 세션이 배포할 때)
- 이 PC에 **Supabase CLI는 brew 설치 실패**(CLT/macOS 26 이슈). 대신 바이너리 직접 설치됨:
  - 경로: `~/.local/share/supabase/supabase` (+ `supabase-go` 동봉). 실행 전 `export PATH="$HOME/.local/share/supabase:$PATH"`.
- **배포에는 로그인 필요**: `supabase login --token <PAT>`. 토큰은 PC에 저장돼 한 번 로그인하면 폐기 전까지 유지된다(매번 재로그인은 이전 토큰을 폐기했기 때문). 보안상 작업 후 토큰 폐기를 권장하나, 폐기하면 다음 배포 시 재로그인 필요.
- 배포 명령: `supabase functions deploy <fn> --project-ref vjoxzbxcylqyhxezxiuj --no-verify-jwt` (fn = submit-reservation / solapi-reservations / admin-reservations)
- 함수 스모크: 틀린 비번 POST → `401` / submit-reservation 잘못된 번호 → `400` 한글 메시지면 정상.

## 4. 다음에 할 일 (우선순위)

0. **✅ 관리자 월간 캘린더 UI — 구현·테스트 완료(2026-06-15)**. push 시 GitHub Pages 자동 배포. 백관장이 관리자 화면에서 캘린더 등록/수정/삭제·행 선택 동작 실제 확인만 남음.
1. **✅ 1차 구조 개선 — 코드·배포·DB 적용·검증 전부 완료(2026-06-13)**
   - 함수 3개 배포(submit-reservation 신규 + solapi/admin 재배포), 스모크 통과.
   - 라이브 DB: `anon can create reservation` 정책 제거(anon 직접 insert가 RLS 42501로 차단됨을 실제 확인) + `reservations_active_unique` 인덱스 생성 완료. 인덱스 생성 전 중복 활성 신청 1건은 최초만 남기고 취소 처리.
   - (admin-auth 라이브 함수 삭제는 선택 사항으로 남음: `supabase functions delete admin-auth --project-ref vjoxzbxcylqyhxezxiuj`.)
   - ⚠️ 이번 세션에서 `supabase login`에 쓴 PAT가 채팅에 노출됨 — 보안상 대시보드에서 폐기 권장(폐기 시 다음 배포에 재로그인 필요).
2. **문자 자동화 + 현황판 — 구현·배포 완료. 실발송 수동 검증만 남음**
   - 세 Edge Function 모두 최신 코드로 배포됨(2026-06-07). 인증 게이트 스모크 통과.
   - **남은 것(운영자 폰으로):** ① 결제완료 처리 → message_logs에 리마인드·복습 2건 `scheduled` + Solapi 콘솔 예약 2건 / ② 취소 처리 → 두 건 `cancelled` + Solapi 예약 사라짐 / ③ 여석 안내 → 문자 수신 + payment_target 전환 / ④ 현황판에서 발송/예약/제외 표기·재발송 버튼 동작.
3. **테스트 데이터 정리** — 라이브 DB에 테스트 신청(이쌍칼/구마적, 06-06)과 테스트 수업(06-13 01:00)이 있음. 실제 오픈 전 admin에서 정리.
4. (선택) 커스텀 도메인 연결.
5. **공개 전 백관장 승인**(AGENTS.md 14항).

## 5. 주의 (회귀 방지)

- 계약 테스트(`tests/test_static_pages.py`, 17개)가 특정 문자열 존재/금지를 강제. 문구·마크업 바꾸면 함께 확인.
- 어드민에서 기술 용어 노출 금지(테스트가 요구하는 `Supabase`/`ADMIN_PASSWORD_HASH`/함수경로는 JS 주석·상수로만 유지).
- 시크릿 클라이언트 노출 금지(anon publishable 키만 프론트에).
